Toggle switch — for settings and notification preferences.

```jsx
<Switch label="Email notifications" defaultChecked />
<Switch label="Auto-sync earnings" />
```

Props: `label`, plus native checkbox attributes (`checked`, `defaultChecked`, `onChange`, `disabled`). The thumb pops with a spring ease on toggle.
