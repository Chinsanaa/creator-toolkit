# Select

Styled native <select> with custom chevron.

## Props
- `options`: array of `{value, label}` or strings
- `children`: alternative to options (plain <option> elements)
- `label`, `hint`, `error`: same as Input
